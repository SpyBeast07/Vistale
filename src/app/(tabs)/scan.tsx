import { ScanScreen } from '../../screens/ScanScreen';

/**
 * ScanRoute
 * 
 * Entry point for the AR Scanning experience within the Tab navigation.
 */
export default function ScanRoute() {
  return <ScanScreen />;
}
